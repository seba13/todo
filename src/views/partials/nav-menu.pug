
include ../config/links.pug



.wrapper-topbar 
    .topbar
        #hamburger.hamburger
            span
            span 
            span
        a.logo(href=''): img(src="./img/fox.svg", alt="") 

nav#nav.nav--hidden 
    ul.nav-menu
        each link in mainLinks
            -tag = link.icon.tag
            -flag = selected == link.selected
            h1 #{selected}
            li.nav-menu__item: a(href=link.url class=link.icon.class ) 
                #{link.icon.tag}(icon=link.icon.icon) 
                span #{link.name}
               
            //- link.name
    ul.nav-menu
        if(!user)
            each link in linksUser
                li.nav-menu__item: a(href=link.url )
                    #{link.icon.tag}(icon=link.icon.icon) 
                    span #{link.name}  
        else 
            each link in logout
                li.nav-menu__item: a(href=link.url)
                    #{link.icon.tag}(icon=link.icon.icon) 
                    span #{link.name}

